# Shell scripts must keep LF endings. A CRLF script checked out on Linux fails
# with "bad interpreter: /bin/bash^M", which is a baffling error to debug.
*.sh    text eol=lf
Makefile text eol=lf
*.yml   text eol=lf
*.sql   text eol=lf
*.go    text eol=lf
